module.exports = {
    // The test environment that will be used for testing
    testEnvironment: "node",
    preset: 'ts-jest',
    bail: false,
    maxWorkers: 1,
    transformIgnorePatterns: ['^.+\\.js$']
  };
  